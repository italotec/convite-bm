// Preenchido pelo Convite BM ao gerar o ZIP de download (app/routes/extension_bp.py).
// Os valores abaixo são apenas placeholders para desenvolvimento local.
//
// `self` (não `window`) — este arquivo é carregado via importScripts() dentro do service worker
// (background.js), onde `window` não existe.
self.__CB_CONFIG__ = {
  apiKey: "",
  baseUrl: "http://localhost:5020",
  email: ""
};
